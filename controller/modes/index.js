import { selectMode } from './select'
import { appendMode } from './append'
import { contentMode } from './content'

export default {
  select: selectMode,
  append: appendMode,
  content: contentMode,
}
