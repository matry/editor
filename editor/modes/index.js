import { normalMode } from './normal'
import { projectMode } from './project'
import { appendMode } from './append'

export default {
  normal: normalMode,
  append: appendMode,
  project: projectMode,
}
